#!upstart
description "<%= node[:appname] %> server"
author      "slloyd"

start on startup
stop on shutdown

script
  sudo -u vagrant <%= node[:nodejs][:dir] %>/bin/node /<%= node[:appname] %>/app.js >> /var/log/node.log 2>&1 
end script
