#!upstart
description "<%= node[:appname] %> server"
author      "slloyd"

start on startup
stop on shutdown

script
  export HOME="/root"
  exec sudo -u vagrant sh -c "exec <%= node[:nodejs][:dir] %>/bin/node /<%= node[:appname] %>/app.js >> /var/log/node.log 2>&1"
end script
