upstream <%= node[:appname] %> {
  server <%= node[:hostname] %>:<%= node[:app_port] %>;
}

server {
  listen 80;
  server_name <%= node[:hostname] %>

  access_log <%= node[:nginx][:log_dir] %>/<% node[:appname] %>.access.log;

  location / {
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $http_host;
    proxy_set_header X-NginX-Proxy true;

    proxy_pass http://<%= node[:appname] %>;
    proxy_redirect off;
  }
}

